import { FilterKeyPipe } from './filter-key.pipe';

describe('FilterKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
