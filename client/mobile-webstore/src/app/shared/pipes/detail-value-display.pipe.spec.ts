import { DetailValueDisplayPipe } from './detail-value-display.pipe';

describe('DetailValueDisplayPipe', () => {
    it('create an instance', () => {
        const pipe = new DetailValueDisplayPipe();
        expect(pipe).toBeTruthy();
    });
});
